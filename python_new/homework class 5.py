# Write a Python program to convert temperatures to and from celsius, Fahrenheit.
#[ Formula : c/5 = f-32/9 [ where c = temperature in celsius and f = temperature in Fahrenheit]
Fahrenheit = int(input("enter the temp of fahrenheit: "))
celsius= (5*Fahrenheit-160)/9
print(celsius)

#Write a Python program that accepts a string and calculate the number of digits.

anirban = input("please enter the string: ")
print(len(anirban))  # len is a built-in function to find the length of a string